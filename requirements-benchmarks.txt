# --------------------------------------
# Optional dependencies for benchmarks
# Install with: pip install -r requirements-benchmarks.txt
# --------------------------------------

# --- Web frameworks for comparison ---
fastapi==0.95.2  # Last version before Pydantic v2 support
aiofiles==24.1.0
python-multipart==0.0.20
pydantic==1.10.15
uvicorn==0.23.2
flask
django
gunicorn  # WSGI server for Flask and Django

# --- System information collection ---
psutil==6.1.0  # For collecting system specs (CPU, RAM, etc.)

# --- Data processing and plotting ---
numpy==1.24.4
pandas==2.0.3
matplotlib==3.7.5
seaborn==0.13.2
pillow==10.4.0
kiwisolver==1.4.5  # required by matplotlib

# --- For sending HTTP requests in custom tests ---
requests==2.31.0
